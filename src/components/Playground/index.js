import Auth from './Auth';
import './index.scss';

const Playground = () => {
    return (
        <div className='playground'>
            <Auth />
        </div>
    );
}

export default Playground;
